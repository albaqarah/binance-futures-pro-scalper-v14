# Binance Futures Pro Scalper v14

Sanitized GitHub backup. No real API keys included.

## VPS install
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip wheel setuptools
pip install -r requirements.txt
cp .env.example .env
nano .env
```

Fill API keys manually in .env.
